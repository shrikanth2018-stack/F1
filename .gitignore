# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native builds
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# iOS
ios/
*.xcodeproj/
*.xcworkspace/
Pods/

# Android
android/
*.apk
*.aab

# Environment
.env
.env.local
.env.production

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# EAS
google-services.json
GoogleService-Info.plist
play-store-service-account.json

# TypeScript
*.tsbuildinfo

# Metro
.metro-health-check*

# Supabase CLI working state
supabase/.temp/

# Coverage
coverage/

# One-off document generator scripts & outputs
generate_*.js
APP_REVIEW.*
TECHNICAL_AUDIT.*
Old Audits/

# Local-only docs / scratchpads (kept out of git per cleanup batch 2026-05-04)
# PHASE1 audit outputs are historical context — useful locally, no need in repo.
PHASE1_*.md
# Dcos/ holds duplicate / scratchpad copies of canonical docs that already
# live in /docs/ and /MASTER_DOCUMENT.md. Avoiding the drift surface.
Dcos/

# Claude Code local config
.claude/
# Local scratch scripts
scripts/

# Husky auto-generated stubs (regenerated by `npm install` via `prepare` script)
.husky/_/

# Files marked for deletion, kept here as a reversible staging area
_marked_for_deletion/

# Master document (board / investor doc, sensitive business detail)
# Sourced exclusively from code + Supabase + installed app.
# Build a single .docx via scripts/build-master-docx.sh (scripts/ also gitignored).
docs/master/
